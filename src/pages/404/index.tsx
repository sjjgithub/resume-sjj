import React from 'react';
import * as Loadable from "react-loadable";
export default class NotFound extends React.Component<Loadable.LoadingComponentProps> {
    render() {
        return (
            <div>404</div>
        )
    }
}
